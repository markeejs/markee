import {
  autoUpdate,
  computePosition,
  flip,
  offset,
  shift,
  size,
} from '@floating-ui/dom'

export type { Middleware, Placement } from '@floating-ui/dom'

export const floatingUi = {
  autoUpdate,
  computePosition,
  flip,
  offset,
  shift,
  size,
}
